/**
 * PlayerRepository
 * entities層のPlayerRepositoryを段階移行のため再公開する
 * 既存import互換を維持して移行コストを抑える
 */

/** entities層のPlayerRepositoryを互換再エクスポートする */
export { PlayerRepository } from "@client/scenes/game/entities/player/PlayerRepository";