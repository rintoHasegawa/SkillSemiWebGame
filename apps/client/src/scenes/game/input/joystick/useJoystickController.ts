/**
 * useJoystickController
 * hooks配下のuseJoystickControllerを段階移行のため再公開する
 * 既存import互換を維持して段階的な参照置換を可能にする
 */

/** hooks配下のフックを互換再エクスポートする */
export { useJoystickController } from "./hooks/useJoystickController";
