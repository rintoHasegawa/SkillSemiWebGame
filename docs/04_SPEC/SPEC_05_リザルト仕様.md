# リザルト仕様 (Result Screen Specification)

## 画面遷移 (Screen Flow)

### 遷移タイミング (Transition Timing)

- ゲーム終了後，サーバーからゲーム結果データを受信した時点で自動遷移する
- 結果データの受信前は「結果を読み込み中...」を表示する

### リザルト表示の段階 (Result Display Stages)

1. 初期状態: 背景演出 +「結果発表」タイトル +「Tap To Result」ガイドを表示する
1. タップ後: タブバーと順位テーブルが表示される

### セッションの初期化 (Session Reset)

- 結果データを受信した時点で，クライアントは明示の退室イベント（`LEAVE_ROOM`）を送信してルームから抜ける．ソケットは切断しない
  - 終了した試合の席と復帰予約をサーバーに残さないため
- そのため「タイトルへ戻る」ボタンでは退室要求を送らず，画面をタイトルへ戻すだけでよい
- リザルト画面での切断は無視する（この時点でサーバー上のセッションを利用していないため）

## 背景演出 (Background Effect)

### マップ表示 (Map Display)

- ゲーム最終盤面のグリッドをセルごとにチームカラーで描画する
- 未塗装セルは既定の背景色で表示する
- 1位チームのカラーがタイトルテキストの装飾色に使用される

### オーバーレイ (Overlay)

- 背景の上に暗色の半透明オーバーレイを重ね，テキストの視認性を確保する

## タブ構成 (Tab Structure)

### タブ一覧 (Tab List)

リザルト画面は3つのタブで構成される．

#### チーム順位タブ (Team Ranking Tab)

- アイコン: トロフィー
- 表示内容: チーム別の順位と塗り面積率
- データ: 順位，チーム名，塗り面積率（小数点第1位まで表示）
- 既定で選択されるタブ

#### 塗り回数タブ (Paint Count Tab)

- アイコン: パレット
- 表示内容: プレイヤー個人の塗りセル数ランキング
- データ: プレイヤー名，チーム名，塗り回数
- 塗り回数の降順でソートされる

#### ヒット数タブ (Hit Count Tab)

- アイコン: ボム
- 表示内容: プレイヤー個人のボムヒット数ランキング
- データ: プレイヤー名，チーム名，ヒット数
- ヒット数の降順でソートされる

## アクションバー (Action Bar)

### 表示条件 (Display Conditions)

- タブバーと同時に表示される（タップ後）

### ボタン (Buttons)

- 「タイトルへ戻る」ボタン: タイトル画面に遷移する
- 「マップを見る」ボタン: 背景のマップ表示を拡大プレビューする

## 結果データ仕様 (Result Data)

- 保持期間: 結果データはリザルト表示中のみ保持し，ロビーへ遷移した時点で破棄する

### サーバーから受信するデータ (Data Received from Server)

- rankings: チーム別の順位データ（順位，チームID，塗り面積率）
- playerStats: プレイヤー別の成績データ（プレイヤー名，チームID，塗り回数，ボムヒット数）
- finalGridColors: ゲーム終了時点のグリッド全セルのチームID配列
